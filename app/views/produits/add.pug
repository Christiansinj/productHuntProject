extends ../layout

block content
	form(action="/add",method="POST")
		input(type='text', name='nom', placeholder="nom la sociéter")
		input(type='text', name='prenom', placeholder="nom du produit")
		button(type="submit") add
